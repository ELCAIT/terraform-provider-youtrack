---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "youtrack_project_time_tracking_settings Resource - youtrack"
subcategory: ""
description: |-
  YouTrack project time tracking settings resource. Manages time tracking configuration for a project.
---

# youtrack_project_time_tracking_settings (Resource)

YouTrack project time tracking settings resource. Manages time tracking configuration for a project.

## Example Usage

```terraform
# Each project has exactly one time tracking settings object - this resource configures it.
# Destroying this resource disables time tracking for the project (the settings object is not deleted).
#
# estimate_field_name and time_spent_field_name refer to the names of custom fields already attached
# to the project via youtrack_project_custom_field.

resource "youtrack_project" "parent" {
  name         = "My Project"
  short_name   = "MP"
  leader_login = "john.doe"
}

# Attach the "Spent time" field first so it can be referenced in time tracking settings
resource "youtrack_project_custom_field" "spent_time" {
  project_id = youtrack_project.parent.id
  field_name = "Spent time"
  field_type = "PeriodProjectCustomField"
}

resource "youtrack_project_time_tracking_settings" "example" {
  project_id            = youtrack_project.parent.id
  enabled               = true
  time_spent_field_name = youtrack_project_custom_field.spent_time.field_name
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `enabled` (Boolean) Whether time tracking is enabled for this project.
- `project_id` (String) The entity ID of the project.

### Optional

- `estimate_field_name` (String) The name of the custom field attached to the project to use for estimation.
- `time_spent_field_name` (String) The name of the custom field attached to the project to use for tracking spent time.

### Read-Only

- `id` (String) The entity ID of the project time tracking settings.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/sh
# Import project time tracking settings using the project entity ID
terraform import youtrack_project_time_tracking_settings.example <PROJECT_ENTITY_ID>
```
