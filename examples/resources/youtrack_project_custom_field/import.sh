#!/bin/sh
# Import a project custom field attachment using the composite ID: {project_id}/{field_attachment_id}
terraform import youtrack_project_custom_field.priority <PROJECT_ID>/<FIELD_ATTACHMENT_ID>
